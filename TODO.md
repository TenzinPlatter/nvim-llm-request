# TODO
- implement LSP symbols
- fix bug freezing nvim when creating request on empty line (probably when trying to insert we index something we can't?)
- ahhhhhhhhh memory leak!!!!!!!!! (after freeze it seems?)
- it is just pasting random json like this:
```json
{
  "name":"load_global_gl",
  "parameters": {"p_name":""}
}
```
